import { Box } from "@mui/material";

export default function Header() {
    return (
        <Box className={"row"}>
            <p className={"page-header"}>TRAINING CALENDAR 🗂</p>
        </Box>
    );
}
